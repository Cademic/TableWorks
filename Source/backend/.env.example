# -------------------------------------------------------
# ASideNote – Environment Variables
# Copy this file to .env and adjust values as needed.
# In production, set these directly in Render dashboard.
# -------------------------------------------------------

# --- PostgreSQL ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=asidenote
DB_USER=postgres
DB_PASSWORD=postgres

# --- ASP.NET Core ---
# ASPNETCORE_ENVIRONMENT=Development

# --- JWT ---
# JWT_SECRET=your-long-random-secret-here-min-32-chars

# --- CORS ---
# Comma-separated allowed origins (no trailing slashes)
# CORS_ORIGINS=http://localhost:5173

# --- Google OAuth ---
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- Resend (email provider) ---
# RESEND_API_KEY=re_xxxxxxxxxxxx
# EMAIL_FROM_ADDRESS=noreply@yourdomain.com
# EMAIL_FROM_NAME=ASideNote

# --- App URLs (used for verification links, OAuth callbacks) ---
# FRONTEND_URL=http://localhost:5173
# BACKEND_URL=http://localhost:5000
