# -------------------------------------------------------
# ASideNote Frontend – Environment Variables
# Copy this file to .env.local and adjust values as needed.
# -------------------------------------------------------

# API base URL (no trailing slash).
# In development, the Vite dev-server proxy handles /api → localhost:5000.
# In staging/production, set to the full API origin.
VITE_API_BASE_URL=

# Google OAuth client ID (from Google Cloud Console).
# Add your frontend origin (e.g. http://localhost:5173) to the OAuth 2.0 client's
# "Authorized JavaScript origins" to avoid "origin not allowed for the given client ID".
VITE_GOOGLE_CLIENT_ID=
